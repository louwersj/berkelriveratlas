# GeoJSON Indexes

`app/data/index/map.objects.geojson` is the primary published map entry point.

It contains lightweight feature records with:

- geometry
- multilingual title and summary
- timeline metadata
- content path
- source count
- preview image path when available
- language availability

Base geography and river display layers live in `app/data/geo/`.

