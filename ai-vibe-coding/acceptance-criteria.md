# Acceptance Criteria

- The static app builds and runs locally.
- Map, timeline, Markdown content, and graph data load from static files.
- The default public build requires no credentials.
- Validation and release commands succeed.
- Documentation and AI guidance folders are present.

