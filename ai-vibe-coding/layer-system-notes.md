# Layer System Notes

- Local layers must be enough for the default build.
- External layers are optional and clearly labeled.
- Attribution matters.
- Google and 3D layers stay disabled unless explicitly configured locally.

